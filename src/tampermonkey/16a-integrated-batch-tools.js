/************************************************************
 * CONSOLIDATED BATCH TOOL INTEGRATIONS
 ************************************************************/
const UPCOMING_INPUT_KEY = 'cn_upcoming_batch_input_v1';
const UPCOMING_OUTPUT_KEY = 'cn_upcoming_batch_output_v1';
const UPCOMING_DEBUG_KEY = 'cn_upcoming_batch_debug_v1';
const UPCOMING_CONCURRENCY_KEY = 'cn_upcoming_batch_concurrency_v1';
const GV_STORAGE_KEY = 'gv_batch_sender_state_v1';
const UPCOMING_DEFAULT_COURT_ID = 'CT21';
const UPCOMING_SWEEP_MAX = 250;
let upcomingStopRequested = false;
let upcomingActiveRun = null;
let gvQueue = [];
let gvFocusedPair = null;
function upcomingStorageGet(key) {return localStorage.getItem(key) || '';}
function upcomingStorageSet(key,value) {localStorage.setItem(key,String(value || ''));}
function normalizeUpcomingCourtId(courtId) {const raw = norm(courtId || '').toUpperCase(); if (!raw) return ''; if (/^CT[0-9A-Z]+$/.test(raw)) return raw; if (/^\d+$/.test(raw)) return `CT${raw}`; return raw;}
function upcomingConcurrency(raw) {const n = Number.parseInt(String(raw ?? ''),10); return Number.isFinite(n) ? Math.max(1,Math.min(10,n)) : 1;}
function upcomingDebug(event,details = {}) {const row = {at:new Date().toISOString(),event:norm(event),...details}; const old = upcomingStorageGet(UPCOMING_DEBUG_KEY); upcomingStorageSet(UPCOMING_DEBUG_KEY,old ? `${old}\n${JSON.stringify(row)}` : JSON.stringify(row));}
function upcomingSetStatus(msg) {uiStatus(msg); const el = document.getElementById('moUpcomingStatus'); if (el) el.textContent = msg;}
function parseUpcomingInput(text) {const seen = new Set(); const rows = []; for (const line of String(text || '').split(/\r?\n/)) {const s = norm(line); if (!s) continue; const m = s.match(/^([A-Z0-9-]+)\s*(?:[|,\/]|\s+COURT\s*ID\s*:?)\s*([A-Z0-9]+)$/i); const caseNumber = norm((m ? m[1] : s)).toUpperCase(); const courtId = norm(m ? m[2] : '').toUpperCase(); const key = `${caseNumber}|${courtId}`; if (!caseNumber || seen.has(key)) continue; seen.add(key); rows.push({caseNumber,courtId});} return rows;}
function upcomingEnsureRunning() {if (upcomingStopRequested) throw new Error('Stopped by user');}
function upcomingParseCaseFromUrl(u) {try {const url = new URL(u,location.href); const sp = url.searchParams; const caseNumber = sp.get('caseNumber') || sp.get('inputVO.caseNumber') || sp.get('ci') || ''; const courtId = sp.get('courtId') || sp.get('inputVO.courtId') || sp.get('courtCode') || sp.get('l') || ''; const cn = norm(caseNumber).toUpperCase(); const ct = norm(courtId).toUpperCase(); if (!cn || !ct) return null; url.hash = ''; return {caseKey:`${cn}|${ct}`,caseNumber:cn,courtId:ct,url:url.toString()};} catch {return null;}}
function upcomingExtractCaseLinks(htmlText) {const out = []; const seen = new Set(); const doc = new DOMParser().parseFromString(String(htmlText || ''),'text/html'); for (const a of [...doc.querySelectorAll('a[href*="caseNumber="],a[href*="inputVO.caseNumber="],a[href*="ci="]')]) {const parsed = upcomingParseCaseFromUrl(a.href || ''); if (!parsed?.caseKey || seen.has(parsed.caseKey)) continue; seen.add(parsed.caseKey); out.push(parsed);} return out;}
function upcomingExtractCourtIds(text) {const out = new Set(); const src = String(text || ''); const patterns = [/(?:inputVO\.courtId|courtId|courtCode|[?&]l)=([A-Z]{2}[0-9A-Z]+)/gi,/(?:inputVO\.courtId|courtId)\s*[:=]\s*['"]([A-Z]{2}[0-9A-Z]+)['"]/gi,/name=["']inputVO\.courtId["'][^>]*value=["']([A-Z]{2}[0-9A-Z]+)["']/gi]; for (const re of patterns) {let m; while ((m = re.exec(src))) out.add(norm(m[1]).toUpperCase());} return [...out];}
async function upcomingFindCandidates(caseNumber,signal) {upcomingEnsureRunning(); const url = new URL('/casenet/caseNoSearch.do',location.origin); const body = new URLSearchParams(); body.set('caseNumber',caseNumber); body.set('inputVO.caseNumber',caseNumber); body.set('newSearch','Y'); const resp = await fetch(url.toString(),{method:'POST',credentials:'include',headers:{'Content-Type':'application/x-www-form-urlencoded; charset=UTF-8'},body:body.toString(),signal}); const txt = await resp.text(); upcomingDebug('case_number_search_response',{caseNumber,httpStatus:resp.status,ok:!!resp.ok,responseLength:txt.length}); if (!resp.ok) throw new Error(`caseNoSearch HTTP ${resp.status}`); return upcomingExtractCaseLinks(txt);}
async function upcomingPostJson(path,bodyObj,qs = '',signal) {upcomingEnsureRunning(); const url = new URL(path,location.origin); if (qs) url.search = qs; const body = new URLSearchParams(); for (const [k,v] of Object.entries(bodyObj || {})) body.set(k,String(v ?? '')); const resp = await fetch(url.toString(),{method:'POST',credentials:'include',headers:{'Content-Type':'application/x-www-form-urlencoded; charset=UTF-8','Accept':'application/json,text/javascript, */*; q=0.01','X-Requested-With':'XMLHttpRequest'},body:body.toString(),signal}); const txt = await resp.text(); const ct = String(resp.headers.get('content-type') || '').toLowerCase(); upcomingDebug('json_post_response',{path,httpStatus:resp.status,ok:!!resp.ok,responseLength:txt.length,contentType:ct,redirected:!!resp.redirected}); if (!resp.ok) throw new Error(`${path} HTTP ${resp.status}`); try {return JSON.parse(txt);} catch {} const trimmed = String(txt || '').trim(); const jsonStart = Math.max(trimmed.indexOf('{'),trimmed.indexOf('[')); if (jsonStart >= 0) {try {return JSON.parse(trimmed.slice(jsonStart));} catch {}} const err = new Error(`${path} non-JSON response`); err.meta = {responseText:txt}; throw err;}
function upcomingDateFromDocket(docketList) {const today = new Date(); today.setHours(0,0,0,0); const all = []; for (const entry of docketList || []) for (const s of (entry?.associatedDocketScheduledInfo || [])) {const d = norm(s?.associatedDate || ''); if (!/^\d{2}\/\d{2}\/\d{4}$/.test(d)) continue; const [mm,dd,yyyy] = d.split('/').map(Number); const dt = new Date(yyyy,mm - 1,dd); dt.setHours(0,0,0,0); all.push({dt,d,time:norm(s?.associatedTime || ''),judge:norm(s?.associatedName || ''),locationText:norm(s?.associatedText || '')});} all.sort((a,b) => a.dt - b.dt); const next = all.find((x) => x.dt.getTime() >= today.getTime()); return next ? [next.d,next.time || '- - -',next.judge || '- - -',next.locationText || '- - -'].join(' ; ') : 'No Upcoming Court Date Found';}
function upcomingHeaderUrl(caseNumber,courtId) {const url = new URL('/casenet/cases/newHeader.do',location.origin); url.searchParams.set('inputVO.caseNumber',caseNumber); url.searchParams.set('inputVO.courtId',normalizeUpcomingCourtId(courtId || UPCOMING_DEFAULT_COURT_ID)); url.searchParams.set('inputVO.isTicket','false'); return url.toString();}
async function scrapeUpcomingDateIntegrated({caseNumber,courtId},signal) {const candidates = await upcomingFindCandidates(caseNumber,signal).catch(() => []); const order = []; const seen = new Set(); const push = (v) => {const ct = normalizeUpcomingCourtId(v); if (ct && !seen.has(ct)) {seen.add(ct); order.push(ct);}}; push(courtId); push(UPCOMING_DEFAULT_COURT_ID); push('CT22'); candidates.map(c => c.courtId).forEach(push); for (let i = 1; i <= UPCOMING_SWEEP_MAX; i++) push(`CT${i}`); let header,lastErr; for (const ct of order) {try {header = await upcomingPostJson('/casenet/cases/newHeaderData.do',{caseNumber,courtId:ct,isTicket:'',locnCode:'',isCriminal:'',diposed:'',pleaAndPayInd:''},'',signal); break;} catch (err) {lastErr = err; for (const extra of upcomingExtractCourtIds(err?.meta?.responseText || '')) push(extra);}} if (!header) throw lastErr || new Error('Unable to resolve header data'); const resolvedCourtId = normalizeUpcomingCourtId(header?.courtId || courtId || ''); const docket = await upcomingPostJson('/casenet/cases/docketEntriesSearch.do',{caseNumber,courtId:resolvedCourtId,isTicket:'',tabName:'Docket'},'displayOption=A&sortOption=D&hasChange=false',signal); return {upcoming:upcomingDateFromDocket(docket?.docketTabModelList || []),resolvedCourtId,resolvedHeaderUrl:upcomingHeaderUrl(caseNumber,resolvedCourtId)};}
async function runUpcomingBatchIntegrated() {const $input = document.getElementById('moUpcomingInput'); const $output = document.getElementById('moUpcomingOutput'); const $conc = document.getElementById('moUpcomingConcurrency'); const requested = parseUpcomingInput($input?.value || ''); if (!requested.length) {upcomingSetStatus('Enter at least one case number.'); return;} upcomingStorageSet(UPCOMING_INPUT_KEY,$input.value); const conc = upcomingConcurrency($conc?.value); upcomingStorageSet(UPCOMING_CONCURRENCY_KEY,String(conc)); upcomingStopRequested = false; upcomingActiveRun = {controller:new AbortController()}; upcomingDebug('batch_start',{requestedCount:requested.length,concurrency:conc}); const results = new Array(requested.length); let next = 0, done = 0; async function worker() {while (next < requested.length) {const i = next++; const item = requested[i]; try {const scraped = await scrapeUpcomingDateIntegrated({caseNumber:item.caseNumber,courtId:normalizeUpcomingCourtId(item.courtId || UPCOMING_DEFAULT_COURT_ID)},upcomingActiveRun.controller.signal); results[i] = `${scraped.upcoming} ; ${item.caseNumber}`; upcomingDebug('batch_case_complete',{caseNumber:item.caseNumber,courtId:scraped.resolvedCourtId,result:scraped.upcoming});} catch (err) {results[i] = `Error (${String(err?.message || err)}) ; ${item.caseNumber}`; upcomingDebug('batch_case_error',{caseNumber:item.caseNumber,message:String(err?.message || err)});} done++; upcomingSetStatus(`Processed ${done}/${requested.length} case number(s) (concurrency ${conc})...`); await sleep(100);}} await Promise.all(Array.from({length:Math.min(conc,requested.length)},worker)); $output.value = results.join('\n'); upcomingStorageSet(UPCOMING_OUTPUT_KEY,$output.value); upcomingActiveRun = null; upcomingSetStatus(upcomingStopRequested ? `Stopped. ${results.filter(Boolean).length} result(s) collected.` : `Done. ${results.length} result(s).`);}
function renderUpcomingCourtDates() {addBlock('Upcoming Court Dates',`<div class="moFeaturePlaceholder"><div class="moField"><label>Case Numbers (newline-separated; optional court ID after pipe/comma)</label><textarea id="moUpcomingInput" placeholder="18SL-CR01234&#10;704581692">${escapeHtml(upcomingStorageGet(UPCOMING_INPUT_KEY))}</textarea></div><div class="moNameRowBottom" style="grid-template-columns:auto 1fr 1fr 1fr;"><input id="moUpcomingConcurrency" type="number" min="1" max="10" value="${escapeHtml(upcomingStorageGet(UPCOMING_CONCURRENCY_KEY) || '1')}" style="height:32px;border:1px solid #94a3b8;border-radius:8px;padding:4px 8px;"><button class="moBtn moSearchBtn" id="moUpcomingRun" style="height:32px;">Find Upcoming Dates</button><button class="moBtn" id="moUpcomingStop" style="height:32px;">Stop</button><button class="moBtn" id="moUpcomingClear" style="height:32px;">Clear Entries</button></div><div class="moField"><label>Results</label><textarea id="moUpcomingOutput" readonly placeholder="Results will appear here">${escapeHtml(upcomingStorageGet(UPCOMING_OUTPUT_KEY))}</textarea></div><div class="moNameRowBottom" style="grid-template-columns:1fr 1fr 1fr;"><button class="moBtn" id="moUpcomingCopy">Copy Results</button><button class="moBtn" id="moUpcomingCopyDebug">Copy Debug</button><button class="moBtn" id="moUpcomingClearDebug">Clear Debug</button></div><div class="moTiny" id="moUpcomingStatus">Ready.</div></div>`);}
function gvLoadState() {try {return JSON.parse(localStorage.getItem(GV_STORAGE_KEY) || '{}') || {};} catch {return {};}}
function gvSaveFromUi() {localStorage.setItem(GV_STORAGE_KEY,JSON.stringify({phones:document.getElementById('moGvPhones')?.value || '',names:document.getElementById('moGvNames')?.value || '',lastNames:document.getElementById('moGvLastNames')?.value || '',message:document.getElementById('moGvMessage')?.value || ''}));}
function gvLines(text) {return String(text || '').split(/\r?\n/).map(s => s.trim()).filter(Boolean);}
function gvPhone(text) {let d = String(text || '').replace(/\D/g,''); if (d.length === 11 && d.startsWith('1')) d = d.slice(1); return d.length === 10 ? d : '';}
function gvFmt(d) {return /^\d{10}$/.test(d) ? `(${d.slice(0,3)}) ${d.slice(3,6)}-${d.slice(6)}` : '';}
function gvCompose(first,base) {const body = String(base || '').trim(); const f = norm(first); if (!body) return f ? `Hi ${f},` : 'Hi,'; return f ? `Hi ${f}, ${body}` : `Hi, ${body}`;}
function gvBuildQueue() {const phones = gvLines(document.getElementById('moGvPhones')?.value || ''); const first = gvLines(document.getElementById('moGvNames')?.value || ''); const last = gvLines(document.getElementById('moGvLastNames')?.value || ''); if (!phones.length) throw new Error('Enter at least one phone number.'); const parsed = phones.map((phone,idx) => ({index:idx + 1,phoneRaw:phone,phoneDigits:gvPhone(phone),phoneDisplay:gvFmt(gvPhone(phone)) || phone.trim(),firstName:norm(first[idx] || ''),lastName:norm(last[idx] || '')})); const bad = parsed.filter(x => !x.phoneDigits); if (bad.length) throw new Error(`These phone numbers could not be parsed: ${bad.map(x => x.phoneRaw).join(', ')}`); return parsed;}
function gvRenderStatus(message) {const status = document.getElementById('moGvStatus'); const preview = document.getElementById('moGvPreview'); if (message && status) status.textContent = message; else if (status && gvFocusedPair) status.innerHTML = `<strong>${escapeHtml([gvFocusedPair.firstName,gvFocusedPair.lastName].filter(Boolean).join(' ') || '(no name provided)')}</strong><br>${escapeHtml(gvFocusedPair.phoneDisplay || gvFocusedPair.phoneRaw)}<br>${gvQueue.length} pair(s) remaining in queue.`; else if (status) status.textContent = 'No pair loaded.'; if (preview) preview.textContent = gvQueue.length ? gvQueue.slice(0,5).map(item => `[${item.index}] ${[item.firstName,item.lastName].filter(Boolean).join(' ') || '(no name)'} -> ${item.phoneDisplay}`).concat(gvQueue.length > 5 ? [`...and ${gvQueue.length - 5} more.`] : []).join('\n') : '(queue preview will appear here)';}
function gvLoadPairs() {gvSaveFromUi(); gvQueue = gvBuildQueue(); gvFocusedPair = gvQueue[0] || null; gvRenderStatus();}
function gvRemoveFirstLine(id) {const el = document.getElementById(id); if (!el) return; const all = String(el.value || '').split(/\r?\n/); while (all.length && !all[0].trim()) all.shift(); if (all.length) all.shift(); el.value = all.join('\n');}
function renderGoogleVoiceAssist() {const st = gvLoadState(); addBlock('Google Voice Assist',`<div class="moFeaturePlaceholder"><div class="moNameRow" style="grid-template-columns:1fr 1fr;"><div class="moField"><label>First names (one per line)</label><textarea id="moGvNames" placeholder="Yasmine&#10;Shauron">${escapeHtml(st.names || '')}</textarea></div><div class="moField"><label>Last names (one per line)</label><textarea id="moGvLastNames" placeholder="Smith&#10;Jones">${escapeHtml(st.lastNames || '')}</textarea></div></div><div class="moNameRow" style="grid-template-columns:1fr 1fr;"><div class="moField"><label>Message body</label><textarea id="moGvMessage" placeholder="this is Mason with Tap In...">${escapeHtml(st.message || '')}</textarea></div><div class="moField"><label>Phone numbers (one per line)</label><textarea id="moGvPhones" placeholder="(314) 302-7864&#10;(314) 435-9029">${escapeHtml(st.phones || '')}</textarea></div></div><div class="moNameRowBottom" style="grid-template-columns:repeat(6,1fr);"><button class="moBtn moSearchBtn" id="moGvLoad">Load Pairs</button><button class="moBtn" id="moGvNumber">Number</button><button class="moBtn" id="moGvText">Text</button><button class="moBtn" id="moGvName">Name</button><button class="moBtn" id="moGvNext">Next</button><button class="moBtn" id="moGvClear">Clear</button></div><div class="moTiny">No auto-sending. Use the helper buttons to copy and step through each name + number pair.</div><div class="moTextBuilderMessage" id="moGvStatus">No pair loaded.</div><pre class="moTextBuilderMessage" id="moGvPreview">(queue preview will appear here)</pre></div>`); setTimeout(() => {try {gvLoadPairs();} catch {gvRenderStatus();}},0);}
